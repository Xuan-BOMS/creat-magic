# AGENTS.md

## 语言与交互

- 与用户交流使用中文。
- 与工具、代码、命令、模型提示交互优先使用英文。
- 除非用户明确要求并二次确认，不要修改本文件。

## 项目位置

项目工作目录：

```text
D:\magic\creat-magic
```

当前已创建：

```text
D:\magic\creat-magic\docs\施法生成逻辑规划.md
D:\magic\creat-magic\AGENTS.md
```

## 项目目标

构建一个“轰界法术生成器”。

本项目不是法术分类百科，也不是元素技能树，而是从施法角度构建法术生成逻辑。用户通过选择并连接不同节点，让系统判断这些节点能否构成一条可执行的法术链路，并输出施法流程、成立条件、代价、风险、失败原因和优化建议。

核心判断：

```text
魔法是理解框架。
法术是可执行流程的结果。
```

因此系统应被设计成“法术编译器”，而不是“法术图鉴”。

## 已参考资料

主要资料：

```text
D:\magic\设定\轰界世界观手册_人工统稿.md
```

少量校准字幕：

```text
D:\B_Downloads\hong\srt\txt_output\魔力其实无处不在？你所不知道的法师魔力真相【魔法科普】.txt
D:\B_Downloads\hong\srt\txt_output\火球术其实真的很简单？你所不知道的法术施法真相【魔法科普】.txt
D:\B_Downloads\hong\srt\txt_output\【魔法科普】元素的本质到底是什么？？.txt
D:\B_Downloads\hong\srt\txt_output\【魔法科普】施法技巧（正统秘术）的原理解析调查报告.txt
D:\B_Downloads\hong\srt\txt_output\【魔法科普】施法技巧（旁门左道）的原理解析调查报告.txt
D:\B_Downloads\hong\srt\txt_output\史上最强最神秘的魔法究竟是什么？？祈愿系魔法的原理大揭秘【魔法科普】.txt
```

全字幕"D:\B_Downloads\hong\srt\txt_output\全视频字幕(截至第五法5.6).txt"
谨慎读取，体量很大

不要无节制读取全部字幕。需要补充资料时，应围绕当前实现问题少量读取。

## 当前进度

已完成：

- 创建项目目录 `D:\magic\creat-magic`。
- 阅读主世界观手册的魔法、元素、祈愿、结界、技巧、职业相关内容。
- 少量参考魔法相关字幕，校准施法流程与节点逻辑。
- 确认实现方向：服务器 + 前端访问模式。
- 编写规划文档：`docs\施法生成逻辑规划.md`。

规划文档已包含：

- 项目目标。
- 资料校准。
- 服务器 + 前端实现选择。
- 产品形态。
- 核心法术链路。
- 节点类型。
- 节点数据结构。
- 连接规则。
- 编译流程。
- 示例法术。
- 后端设计。
- 前端设计。
- 数据优先级。
- 开发阶段。

## 已确定实现方向

优先实现服务器 + 前端工作台。

推荐技术路线：

```text
后端：Python + FastAPI + Pydantic + SQLite
前端：React + TypeScript + Vite + React Flow
部署：Docker Compose，后续可接 Nginx 或 Caddy
```

暂不优先做 exe。若未来需要离线版本，可在 Web 版本稳定后使用 Docker、本地部署、Tauri 或 Electron 包装。

## 核心生成逻辑

普通法术基础链路：

```text
意图 → 介质 → 开模 → 提纯 → 灌注 → 释放 → 结果 → 代价校验
```

祈愿类链路：

```text
意图 → 请求语义 → 祈愿法阵 → 魔网链接 → 神之遗物返还 → 寄托物承载 → 结果 → 歧义与反噬校验
```

结界类链路：

```text
意图 → 环境条件 → 诱因 → 转化 → 回填 → 循环闭合 → 子功能挂载 → 维护校验
```

系统必须优先判断法术能否成立，再生成名称和描述。

## 节点设计原则

节点不应只是分类名，而应具备输入、输出、成本、风险和兼容性。

第一批节点建议只做最小可运行集合：

介质：

```text
火、水、风、土、以太、雷电
```

流程：

```text
开模、提纯、灌注、释放、序列、多重、延迟、复合、附加、结界循环、祈愿
```

结构：

```text
弹体、刃、壁、场域、结界、附魔、契约
```

载体：

```text
手势、咏唱、法杖、魔法阵、魔石、卷轴、寄托物
```

约束：

```text
精神力、注意力、被打断、材料、环境、反噬、外溢、禁术
```

## 第一阶段计划

下一步应进入“后端规则内核”阶段。

目标：

```text
不用前端，也能用命令行或 API 编译一张法术节点图。
```

建议交付：

- 建立后端项目骨架。
- 定义节点数据模型。
- 定义法术图数据模型。
- 建立最小节点库。
- 实现连接校验。
- 实现编译器。
- 实现风险评分。
- 实现法术卡输出。
- 为 4 个示例法术写测试。

建议示例：

```text
火球术
多重风刃
侦查结界
祈愿传送
```

## 后续阶段

阶段二：Web 工作台。

- React Flow 画布。
- 节点库面板。
- 节点属性检查器。
- 编译结果面板。
- 本地草稿保存。

阶段三：项目与版本。

- 服务器保存方案。
- 版本历史。
- 示例模板。
- 节点库版本号。

阶段四：知识库扩展。

- 魔导具。
- 附魔。
- 祈愿细化。
- 契约术。
- 死灵三柱。
- 德鲁伊生态循环。
- 生体改造。
- 信仰侧雷电与光道。

阶段五：服务器部署。

- Docker Compose。
- 反向代理。
- 登录权限。
- 数据备份。
- 管理员维护节点库。

## 工作规则

- 优先保持改动小而可验证。
- 先实现规则内核，再做前端表现。
- 先判断成立条件，再生成描述文本。
- 生成失败结果要清楚说明失败原因。
- 不要把 AI 文案生成当作规则判断来源。
- 节点库要可版本化，避免后续世界观扩展时破坏旧方案。
- 中文文件写入后必须读取校验，确认无乱码。
- 不要修改 `D:\magic\设定` 下的原始资料文件。
- 不要修改本文件，除非用户明确要求并二次确认。
