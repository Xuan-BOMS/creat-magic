# AGENTS.md

## 语言与交互

- 与用户交流使用中文。
- 与工具、代码、命令、模型提示交互优先使用英文。
- 中文文件写入后必须立即用 UTF-8 读取校验，确认无乱码。

## 项目位置

```text
D:\magic\creat-magic
```

## 项目定位

本项目构建“轰界法术生成器”。

它不是法术百科，也不是元素技能树，而是从施法流程角度构建“法术编译器”。玩家选择节点并组织成施法流程，系统先判断链路是否成立，再输出名称、简述、阶段结果、风险、代价、失败原因和优化建议。

核心判断：

```text
魔法是理解框架。
法术是可执行流程的结果。
```

## 当前 MVP 方向

第一版实现固定四阶段：

```text
开模 → 提纯 → 灌注 → 释放
```

MVP 阶段内部使用“有序节点列表”，暂不做自由图/DAG。前端可以用箭头和节点块表现连接关系，但后端按阶段顺序线性编译。

四阶段职责：

- 开模：生成模具或作用形态，例如球形、刃形、线形、范围、墙体。
- 提纯：生成元素或复合属性，例如火、水、风、土、以太操作、泥沼、雷电。
- 灌注：将提纯结果注入开模结构，例如标准灌注、压缩灌注、范围灌注、多重灌注。
- 释放：使已完成的法术结构生效，例如投射释放、激发释放、扩散释放、流动释放、场域释放。

## 运行时数据原则

- `data\magic_have\*.md` 是人工可读资料来源，不作为运行时规则库。
- 运行时节点与示例使用结构化数据：

```text
data\nodes\mvp_nodes.json
data\examples\*.json
```

- 每个节点必须具备稳定 ID、所属阶段、输出、风险标签和评分偏置。
- 节点实例必须区分 `node_id` 与 `instance_id`，允许同一节点重复使用。

## MVP 示例

第一版只覆盖普通四阶段法术：

```text
火球术：单元素普通攻击
多重风刃：普通法术 + 多重灌注
泥沼术：水土复合提纯 + 范围灌注
雷电术：火风水催化复合 + 高风险释放
```

结界、祈愿、契约、附魔不是第一版普通四阶段 MVP 的内容，只预留后续扩展。

## 编译输出

系统必须先判断成立条件，再生成描述文本。

`POST /api/compile-graph` 返回：

- 编译状态：`compiled`、`partial`、`failed`、`unsafe`
- 法术名称与简述
- 四阶段输出
- 定位到阶段和节点实例的错误/警告/高危项
- 法术卡
- 七维评分

七维评分：

```text
威力
稳定性
易学性
魔力效率
泛用性
学术价值
安全性
```

## 延后范围

以下内容不要在 MVP 中提前实现：

- 任意自由图或 DAG 编译。
- 玩家跨阶段手动画边。
- 结界、祈愿、契约、附魔完整体系。
- 项目保存、版本历史、登录权限。
- 服务器部署。
- AI 自动规则判定或 AI 自动命名。
- 从 Markdown 自动生成运行时规则。

## 技术路线

```text
后端：Python + FastAPI + Pydantic
前端：React + TypeScript + Vite
数据：JSON 节点库 + JSON 示例图
```

旧接口 `/api/catalog` 与 `/api/compile` 可作为 legacy 保留；MVP 新接口为：

```text
GET  /api/nodes
GET  /api/examples
POST /api/compile-graph
```

## 前端 UI 设计基准

主工作台页面必须参考：

```text
D:\magic\creat-magic\docs\ui\assets\workbench-main-ui-reference.svg
```

实现或修改前端主工作台后，必须通过浏览器截图、读图观察和人工对照验证布局、信息层级、配色、节点类型色标、右侧检查器、底部编译结果区和整体视觉风格。发现不符合时继续修改并再次验证。

## 协作与子代理规则

- 默认情况下，第三方模型只读输出建议。
- 用户已明确放开本项目 Codex 子代理写权限；主会话可以调度 Codex 子代理直接写入。
- 每个可写子代理必须先被分配明确且不重叠的写入范围，且只能修改该范围内文件。
- 子代理写入后必须报告实际修改文件、验证命令和剩余风险。
- 子代理不得修改 `D:\magic\设定` 下的原始资料。
- 主会话负责最终合并、重构、测试和交付说明。

## 工作规则

- 优先保持改动小而可验证。
- 不要修改 `D:\magic\设定` 下的原始资料文件。
- 不要把 AI 文案生成当作规则判断来源。
- 生成失败结果要清楚说明失败原因。
- 先规则内核，再前端表现，再项目保存。
- 若工作树已有用户改动，必须保留并顺势工作，不得擅自回滚。
